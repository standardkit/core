/** @deprecated Should be moved to ui if still relevant after new style upgrade */
export const enum FilterEnum {
  SingleSelect = 'single-select',
  MultiSelect = 'multi-select',
  Checkbox = 'checkbox',
  Date = 'date',
}
